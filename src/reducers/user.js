import {OrderedMap} from 'immutable'

const initState = {
  models: new OrderedMap(),
}
export default (state = initState, action) => {

  switch (action.type) {

    default:
      return state
  }

}